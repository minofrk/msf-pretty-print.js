{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    /* Basic Options */
    "lib": [
      "esnext"
    ],

    /* Module Resolution Options */
    "types": [
      "jest",
      "node"
    ]
  },
  "include": [
    "**/*.ts",
  ]
}
